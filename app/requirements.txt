# URLGuard FastAPI app — runtime dependencies only.
# Deliberately excludes training-only packages (dvc, jupyter, matplotlib,
# lightgbm, notebook tooling, etc.) that live in the project-root
# requirements.txt — this file is what the app's Docker image installs.

fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
pydantic==2.10.4

# Model loading + inference
mlflow==2.19.0
dagshub==0.5.9
scikit-learn==1.5.2
xgboost==2.1.3
pandas==2.2.3
numpy==1.26.4

# Feature extraction (live page fetch for content-based features)
requests==2.32.3
beautifulsoup4==4.12.3

# Metrics (Prometheus scrape endpoint)
prometheus-client==0.21.1