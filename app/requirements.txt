# URLGuard FastAPI app — runtime dependencies only.
# Versions below are copied directly from the project-root
# requirements.txt (confirmed working together in the real training/
# serving environment) rather than independently chosen — this is what
# fixed the mlflow/skops version-mismatch error from before.
# Deliberately excludes training/notebook-only packages: dvc, awscli,
# boto3 (DVC/S3 concerns, not touched by the serving app), lightgbm,
# matplotlib, jupyter/ipykernel/ipython, Flask/flask-cors/waitress
# (unused — this app is FastAPI, not Flask), and mlflow's own
# transitive tracking-server deps (alembic, sqlalchemy, databricks-sdk,
# opentelemetry-*, graphql/gql, docker, GitPython, etc.) — those install
# automatically as dependencies of mlflow/dagshub below, no need to
# pin them individually here.

fastapi==0.141.1
uvicorn==0.52.4
Jinja2==3.1.6
pydantic==2.13.4

# Model loading + inference — exact versions the model was actually
# logged and tested with.
mlflow==3.15.1
dagshub==0.7.1
scikit-learn==1.9.0
xgboost-cpu==3.2.0
pandas==2.3.3
numpy==2.4.6
psutil==5.9.8
skops==0.14.0

# Feature extraction (live page fetch for content-based features)
requests==2.34.2
beautifulsoup4==4.15.0

# Metrics (Prometheus scrape endpoint)
prometheus_client==0.26.0

# Production server — not in your root requirements.txt because
# gunicorn doesn't install on Windows (your dev machine), but the
# Docker image is Linux (python:3.11-slim), where it works fine.
gunicorn==23.0.0